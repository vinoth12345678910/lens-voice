# models/best

Placeholder directory. The best trained/exported model is copied here from
`experiments/<best>/weights/best.pt` once the benchmark candidate is locked.

See `reports/final_model_report.md` for the template it must satisfy.